name: tech_core_yolo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - torch==2.8.0
      - torchvision==0.23.0
      - ultralytics==8.4.30
      - wandb
      - pyyaml
